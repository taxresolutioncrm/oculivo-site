{
  "name": "oculivo-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run verify:closeout && astro build",
    "preview": "astro preview",
    "verify:closeout": "node scripts/verify-closeout.mjs && node scripts/verify-links.mjs && node scripts/verify-deploy-config.mjs"
  },
  "dependencies": {
    "astro": "7.3.3"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "wrangler": "4.20.0"
  }
}
